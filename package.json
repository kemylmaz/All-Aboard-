{
  "name": "minibus-tycoon",
  "private": true,
  "description": "Oyun frontend/ altinda. Cloudflare Workers Builds bagimlilik kurulumunu depo kokunde calistiriyor; bu paket kokun kurulabilir olmasi icin var ve isi frontend workspace'ine devrediyor.",
  "workspaces": ["frontend"],
  "scripts": {
    "build": "npm --workspace frontend run build",
    "build:cf": "npm --workspace frontend exec opennextjs-cloudflare build",
    "deploy:cf": "npm --workspace frontend exec wrangler deploy"
  }
}
